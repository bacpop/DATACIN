//use ska::ska_dict::SkaDict;
//use ska::{QualOpts, QualFilter};

pub fn build_idx(file: &str) -> usize {
    /*
    let k = 31;
    let sample_idx = 0;
    let quality = QualOpts {min_count: 1, min_qual: 0, qual_filter: QualFilter::NoFilter};
    let ska_dict = SkaDict::<u64>::new(k, sample_idx, (file, None), "test_1", true, &quality);
    ska_dict.ksize()
    */
    2
}
